node_modules/
dist/
*.log
.DS_Store
.env
.remember/

# Generated by scripts/build-store-assets.mjs; the composed PNGs are committed.
store-assets/harness-*.html

# Raw capture sources that show the whole desktop or browser window: they carry
# tabs, bookmarks, profile and personalised recommendations. Only the tightly
# cropped popup (store-assets/raw-popup-live.png) is committed.
store-assets/end-to-end-proof.mov
store-assets/rawpopup-live.png
store-assets/*.mov
store-assets/*.mp4
